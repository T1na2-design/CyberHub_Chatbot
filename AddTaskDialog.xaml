<Window x:Class="CybersecurityChatbot.AddTaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add New Task" Height="300" Width="400"
        WindowStartupLocation="CenterOwner" WindowStyle="ToolWindow"
        Background="#1E1E2E" Foreground="White" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Title:" Foreground="#8BE9FD" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="TitleBox" Background="#44475A" Foreground="White" 
                 Padding="5" Margin="10,0,0,0"/>

        <!-- Description -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" Foreground="#8BE9FD" VerticalAlignment="Top" Margin="0,10,0,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="DescriptionBox" Background="#44475A" Foreground="White" 
                 Padding="5" Margin="10,10,0,0" Height="80" TextWrapping="Wrap" VerticalAlignment="Top"/>

        <!-- Reminder Date -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Reminder:" Foreground="#8BE9FD" VerticalAlignment="Center" Margin="0,10,0,0"/>
        <DatePicker Grid.Row="2" Grid.Column="1" x:Name="ReminderDatePicker" Background="#44475A" Foreground="White" 
                    Margin="10,10,0,0"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Add" Width="80" Background="#50FA7B" Foreground="Black" FontWeight="Bold" Click="AddBtn_Click" Margin="0,0,10,0"/>
            <Button Content="Cancel" Width="80" Background="#FF5555" Foreground="White" FontWeight="Bold" Click="CancelBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
